import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mobile_ionic_typescript_hello-world_angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
